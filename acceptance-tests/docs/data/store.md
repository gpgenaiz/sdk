# Data Store

Store is a command group of workspace which targets data stores available to a workspace flow nodes or that are used
by flow nodes. Managing node stores is split between store and [node](../workspace/node.md). The store command is
strictly for listing store instances.

## store list